'use client';

import { ProfileSettings } from '@components/settings';

// --- BEGIN COMMENT ---
// 个人资料设置页面
// 只负责导入组件，所有逻辑都在组件内部实现
// --- END COMMENT ---
export default function ProfileSettingsPage() {
  return <ProfileSettings />;
}
